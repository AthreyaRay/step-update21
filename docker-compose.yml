services:
  web:
    image: node:14
    volumes:
      - .:/app
    working_dir: /app
    command: /bin/sh -c "npm install && npm start"
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
      interval: 10s
      timeout: 5s
      retries: 5
